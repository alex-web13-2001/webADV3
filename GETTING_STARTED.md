# 🚀 Getting Started Guide

## Быстрый старт

### 1. Установка зависимостей

```bash
# Backend
cd backend
npm install

# Frontend
cd ../frontend
npm install
```

### 2. Настройка окружения

#### Backend (.env)
```bash
cd backend
cp .env.example .env
```

Отредактируйте `.env` при необходимости. Значения по умолчанию подходят для локальной разработки.

#### Frontend (.env.local)
```bash
cd frontend
cp .env.local.example .env.local
```

### 3. Запуск приложения

#### Вариант A: Запуск в режиме разработки

**Backend (терминал 1):**
```bash
cd backend
npm run dev
```

Сервер запустится на `http://localhost:4000`

**Frontend (терминал 2):**
```bash
cd frontend
npm run dev
```

Приложение откроется на `http://localhost:3000`

#### Вариант B: Запуск с Docker

```bash
# Из корневой директории проекта
docker-compose up
```

- Frontend: `http://localhost:3000`
- Backend: `http://localhost:4000`

### 4. Получение API ключа Wildberries

1. Войдите в личный кабинет продавца WB
2. Перейдите в раздел **"Профиль" → "Настройки" → "Доступ к API"**
3. Создайте новый ключ с правами:
   - ✅ Статистика
   - ✅ Реклама (чтение)
4. Скопируйте созданный ключ

**⚠️ Важно:** Ключ отображается только один раз. Сохраните его в безопасном месте.

### 5. Использование приложения

1. Откройте `http://localhost:3000`
2. Введите API ключ Wildberries
3. Нажмите **"Проверить API ключ"**
4. После успешной проверки:
   - Нажмите **"Загрузить кампании"**
   - Выберите кампанию из списка
   - Укажите период дат и nm_id товара
   - Просмотрите статистику и аналитику

## 📊 Основные функции

### Overview (Обзор)
- Общие метрики: показы, клики, CTR, CPC, расходы, заказы
- Графики по дням: показы, клики, CTR, расходы

### Clusters (Поисковые кластеры)
- Таблица с детальной статистикой по поисковым фразам
- ТОП-10 фраз по CTR
- ТОП-10 фраз по расходам

## 🛠️ Команды разработки

### Backend

```bash
npm run dev     # Запуск в режиме разработки
npm run build   # Сборка TypeScript
npm start       # Запуск production версии
npm run lint    # Проверка кода ESLint
```

### Frontend

```bash
npm run dev     # Запуск в режиме разработки
npm run build   # Сборка production версии
npm start       # Запуск production версии
npm run lint    # Проверка кода ESLint
```

## 🔧 Устранение неполадок

### Backend не запускается

1. Проверьте, что порт 4000 свободен:
   ```bash
   lsof -i :4000
   ```

2. Убедитесь, что все зависимости установлены:
   ```bash
   cd backend && npm install
   ```

### Frontend не запускается

1. Проверьте, что порт 3000 свободен:
   ```bash
   lsof -i :3000
   ```

2. Убедитесь, что backend запущен и доступен

3. Проверьте `.env.local`:
   ```
   NEXT_PUBLIC_API_URL=http://localhost:4000/api
   ```

### Ошибка "Invalid API key"

1. Проверьте, что API ключ скопирован полностью
2. Убедитесь, что у ключа есть необходимые права доступа
3. Проверьте, что ключ активен в личном кабинете WB

### Проблемы с CORS

Если видите ошибки CORS в консоли браузера:

1. Убедитесь, что backend запущен
2. Проверьте настройку `FRONTEND_URL` в backend/.env
3. Очистите кеш браузера

## 📝 Полезные ссылки

- [Документация WB API](https://dev.wildberries.ru/)
- [Next.js Documentation](https://nextjs.org/docs)
- [TypeScript Documentation](https://www.typescriptlang.org/docs/)
- [Tailwind CSS](https://tailwindcss.com/docs)

## ⚠️ Ограничения MVP

- ❌ Нет базы данных (все данные в реальном времени)
- ❌ Нет сохранения истории
- ❌ API ключ хранится в sessionStorage (до закрытия вкладки)
- ❌ Только чтение данных (нельзя редактировать кампании)
- ❌ Нет аутентификации пользователей

## 💡 Советы

1. **API ключ безопасен**: Он не отправляется на сторонние серверы, только к WB API через наш backend
2. **Период дат**: Рекомендуется выбирать период не более 30 дней для быстрой загрузки
3. **nm_id товара**: Можно найти в URL карточки товара на WB
4. **Обновление данных**: Данные загружаются по требованию, нажмите "Обновить статистику" для актуализации

## 🤝 Поддержка

Если у вас возникли проблемы:

1. Проверьте раздел "Устранение неполадок" выше
2. Откройте Issue на GitHub
3. Проверьте логи в консоли разработчика (F12)
