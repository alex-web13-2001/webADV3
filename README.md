# WB Ads Analytics Dashboard

> MVP аналитического дашборда для работы с рекламными кампаниями Wildberries

## 📋 Описание

Полнофункциональное веб-приложение для анализа и визуализации данных рекламных кампаний Wildberries:
- ✅ Проверка API ключа и отображение баланса
- ✅ Список всех рекламных кампаний с метриками из fullstats
- ✅ Детальная статистика по каждой кампании (KPI, графики, таблица товаров)
- ✅ Анализ CPM кластеров с ставками и минус-фразами
- ✅ Статистика ключевых фраз для ручных кампаний
- ✅ Аналитика автокампаний (кластеры, минус-фразы, доступные товары)
- ✅ Графики: показы/клики, CTR/CPC, средняя позиция товаров, топ кластеров
- ✅ Работа без базы данных - только живые API-запросы

## 🏗️ Архитектура

Монорепозиторий:
- `/backend` - Node.js + Express + TypeScript (API proxy для WB API)
- `/frontend` - Next.js 14 + TypeScript + Tailwind CSS + Recharts

## 🚀 Требования

- Node.js 20+
- npm или yarn
- API ключ Wildberries с правами на чтение статистики рекламы

## 📦 Установка

### 1. Клонировать репозиторий
```bash
git clone https://github.com/alex-web13-2001/webADV3.git
cd webADV3
```

### 2. Установка зависимостей Backend
```bash
cd backend
npm install
cp .env.example .env
# Отредактируйте .env если нужно изменить порт (по умолчанию 5000)
```

### 3. Установка зависимостей Frontend
```bash
cd ../frontend
npm install
cp .env.local.example .env.local
# Отредактируйте .env.local если backend на другом порту
```

## 🏃 Запуск

### Backend (порт 5000)
```bash
cd backend
npm run dev
```

Сервер запустится на http://localhost:5000

### Frontend (порт 3000)
```bash
cd frontend
npm run dev
```

Приложение откроется на http://localhost:3000

## 🐳 Docker (опционально)

```bash
docker-compose up
```

## 📁 Структура проекта

```
webADV3/
├── backend/              # Express API
│   ├── src/
│   │   ├── controllers/  # Контроллеры для обработки запросов
│   │   ├── services/     # Сервисы для работы с WB API
│   │   ├── routes/       # Маршруты API
│   │   ├── middleware/   # Middleware (валидация, обработка ошибок)
│   │   ├── types/        # TypeScript типы
│   │   └── utils/        # Утилиты и конфигурация
│   └── package.json
├── frontend/             # Next.js приложение
│   ├── app/              # Страницы приложения
│   ├── components/       # React компоненты
│   │   └── dashboard/    # Компоненты дашборда
│   ├── lib/              # Библиотеки (API клиент)
│   └── types/            # TypeScript типы
├── docker-compose.yml
└── README.md
```

## 🔑 Получение API ключа Wildberries

1. Войдите в личный кабинет продавца WB
2. Перейдите в раздел "Доступ к API"
3. Создайте новый ключ с правами на **чтение статистики и рекламы**
4. Скопируйте ключ и используйте его при входе в дашборд
5. Документация WB API: https://dev.wildberries.ru/

## 📚 API Endpoints

### Backend API

**Авторизация:**
- `POST /api/auth/validate` - Проверка API ключа через `/adv/v1/balance`
- `GET /api/balance` - Получение баланса рекламного кабинета

**Кампании:**
- `POST /api/campaigns/list` - Список кампаний с метриками из fullstats
- `POST /api/campaigns/:id/overview` - Детальный обзор кампании
- `POST /api/campaigns/:id/cpm-clusters` - CPM кластеры для товара
- `GET /api/campaigns/:id/manual-keywords` - Ключевые фразы ручной кампании
- `GET /api/campaigns/:id/auto-stats` - Статистика автокампании

**Устаревшие (для совместимости):**
- `POST /api/test-key` - Проверка ключа (устарел, используйте `/api/auth/validate`)
- `GET /api/campaigns` - Список кампаний без статистики
- `GET /api/campaigns/:id/stats` - Статистика кампании
- `POST /api/campaigns/:id/clusters` - Кластеры без ставок

### WB API Endpoints (используются внутри)

Backend проксирует следующие endpoint'ы WB API:
- `/adv/v1/balance` - Баланс
- `/adv/v1/promotion/adverts` - Список кампаний
- `/adv/v3/fullstats` - Полная статистика с товарами
- `/adv/v0/normquery/stats` - Статистика кластеров
- `/adv/v0/normquery/get-bids` - Ставки для кластеров
- `/adv/v0/normquery/get-minus` - Минус-фразы
- `/adv/v1/stat/words` - Ключевые фразы ручной кампании
- `/adv/v2/auto/stat-words` - Статистика автокампании
- `/adv/v1/auto/getnmtoadd` - Доступные товары для автокампании

## 🛠️ Технологии

### Backend
- Node.js 20+
- Express.js - Веб-фреймворк
- TypeScript - Типизация
- Axios - HTTP клиент для WB API
- CORS, Helmet - Безопасность
- Express Rate Limit - Защита от DDoS

### Frontend
- Next.js 14 (App Router) - React фреймворк
- TypeScript - Типизация
- Tailwind CSS - Стили
- Recharts - Графики и визуализация
- Lucide React - Иконки
- Axios - HTTP клиент
- date-fns - Работа с датами

## 🎯 Основные возможности

### 1. Авторизация
- Ввод API ключа Wildberries
- Автоматическая проверка через `/adv/v1/balance`
- Отображение баланса (основной + бонусы)
- Хранение ключа в sessionStorage

### 2. Список кампаний
- Загрузка всех кампаний с fullstats за выбранный период
- Отображение базовых метрик для каждой кампании
- Фильтрация по дате

### 3. Обзор кампании
- **KPI плашки:** показы, клики, CTR, CPC, CPM, расходы, заказы, выручка, ATB, конверсия
- **Графики:**
  - Показы и клики по дням
  - CTR и CPC по дням
  - Средняя позиция товаров (с выбором nm_id)
- **Таблица товаров:** детальная статистика по каждому товару из fullstats
  - nm_id извлекается из `days[].apps[].nms[].nmId`

### 4. CPM Кластеры (для кампаний типа 4, 5, 6, 7)
- Выбор товара (nm_id) из fullstats
- Таблица кластеров с метриками:
  - norm_query, показы, клики, CTR, CPC, CPM
  - Средняя позиция, ATB, заказы
  - Ставка (bid), минус-фраза (minus)
- **Графики:**
  - ТОП-10 кластеров по кликам
  - CTR vs позиция (scatter)
  - Ставка vs CTR (scatter)

### 5. Ключевые фразы (для ручных кампаний)
- Таблица всех ключевых фраз с метриками
- График ТОП-20 фраз по кликам
- Отметка минус-фраз

### 6. Автокампании (тип 8, 9)
- Таблица кластеров с ключевыми словами
- Список минус-фраз
- Доступные товары для добавления

## ⚠️ Ограничения MVP

- **Нет базы данных** - все данные загружаются по запросу
- **Нет истории** - данные не сохраняются
- **API ключ** хранится в sessionStorage (до закрытия вкладки)
- **Только чтение** - нельзя изменять ставки, минус-фразы или настройки кампаний
- **Нет cron** - автоматическое обновление данных не предусмотрено

## 🔒 Безопасность

- Helmet для защиты от веб-уязвимостей
- CORS настроен только для фронтенда
- Rate limiting для защиты от DDoS
- API ключ передается через заголовок `x-api-key`
- Валидация всех входящих данных

## 📝 Комментарии в коде

Все комментарии в коде написаны **на русском языке** согласно требованиям ТЗ.

## 🚦 Обработка ошибок

Все ошибки от WB API возвращаются в унифицированном формате:
```json
{
  "success": false,
  "status": 401,
  "endpoint": "/adv/v1/balance",
  "message": "unauthorized: token is malformed"
}
```

Поддерживаются коды ошибок: 400, 401, 403, 404, 429, 500

## 📄 Лицензия

MIT

## 👤 Автор

alex-web13-2001

---

## 💡 Подсказки

### Как получить nm_id товаров?
nm_id автоматически извлекается из статистики fullstats при загрузке обзора кампании. Выберите кампанию, загрузите статистику, и доступные nm_id появятся в выпадающем списке.

### Какие типы кампаний поддерживаются?
- **Тип 4, 5, 6, 7:** Поиск, каталог, карточка - поддерживают CPM кластеры и ключевые фразы
- **Тип 8, 9:** Автокампании - поддерживают анализ кластеров и минус-фраз

### Почему не загружаются кластеры?
Убедитесь, что:
1. Выбрана кампания типа CPM (4, 5, 6, 7)
2. Загружен обзор кампании
3. Выбран nm_id товара из списка
4. Указан корректный период дат

### Как часто обновляются данные?
Данные загружаются по запросу при нажатии кнопок "Загрузить" или "Обновить". Автоматического обновления нет.
