# WB Ads Realtime Dashboard

> MVP аналитического дашборда для работы с рекламными кампаниями Wildberries

## 📋 Описание

Веб-приложение для визуализации данных рекламных кампаний Wildberries в реальном времени:
- Статистика рекламных кампаний
- Аналитика поисковых кластеров
- Метрики CTR, CPC, CPM, показы, клики, заказы
- Работа без базы данных - только живые API-запросы

## 🏗️ Архитектура

Монорепозиторий:
- `/backend` - Node.js + Express + TypeScript (API proxy)
- `/frontend` - Next.js 14 + TypeScript + Tailwind CSS

## 🚀 Требования

- Node.js 20+
- npm или yarn
- API ключ Wildberries

## 📦 Установка

### 1. Клонировать репозиторий
```bash
git clone https://github.com/alex-web13-2001/webADV3.git
cd webADV3
```

### 2. Установка зависимостей Backend
```bash
cd backend
npm install
cp .env.example .env
# Отредактируйте .env если нужно
```

### 3. Установка зависимостей Frontend
```bash
cd ../frontend
npm install
cp .env.local.example .env.local
# Отредактируйте .env.local если нужно
```

## 🏃 Запуск

### Backend (порт 4000)
```bash
cd backend
npm run dev
```

### Frontend (порт 3000)
```bash
cd frontend
npm run dev
```

Откройте браузер: http://localhost:3000

## 🐳 Docker (опционально)

```bash
docker-compose up
```

## 📁 Структура проекта

```
webADV3/
├── backend/           # Express API
│   ├── src/
│   │   ├── routes/
│   │   ├── controllers/
│   │   ├── services/
│   │   └── middleware/
│   └── package.json
├── frontend/          # Next.js приложение
│   ├── src/
│   │   ├── app/
│   │   ├── components/
│   │   └── lib/
│   └── package.json
├── docker-compose.yml
└── README.md
```

## 🔑 Получение API ключа Wildberries

1. Войдите в личный кабинет продавца WB
2. Перейдите в раздел "Доступ к API"
3. Создайте новый ключ с правами на чтение статистики и рекламы
4. Документация: https://dev.wildberries.ru/

## 📚 API Endpoints

### Backend API
- `POST /api/test-key` - Проверка API ключа
- `GET /api/campaigns` - Список кампаний
- `GET /api/campaigns/:id/stats` - Статистика кампании
- `POST /api/campaigns/:id/clusters` - Поисковые кластеры
- `GET /api/products` - Список товаров (nm_id)

## 🛠️ Технологии

### Backend
- Node.js 20+
- Express.js
- TypeScript
- Axios
- CORS, Helmet

### Frontend
- Next.js 14 (App Router)
- TypeScript
- Tailwind CSS
- shadcn/ui
- Recharts

## ⚠️ Ограничения MVP

- Нет базы данных
- Нет хранения истории
- API ключ хранится в sessionStorage (до закрытия вкладки)
- Только чтение данных (без редактирования кампаний)

## 📝 Лицензия

MIT

## 👤 Автор

alex-web13-2001
